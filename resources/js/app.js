import './bootstrap';

// Alpine.js is bundled inside Livewire v4 (@livewireScripts) — no separate import needed.
// Importing it here would start a second Alpine instance and conflict with Livewire's.
